# Git 커밋 (안전 규칙)

- **사용자가 커밋을 명시적으로 요청하거나 `/commit`을 실행할 때만** 커밋한다.
- 커밋 절차·메시지 형식은 **`.claude/skills/commit/SKILL.md`** (`/commit`)를 따른다.
- `git config` 변경, force push, hook 우회(`--no-verify`), 요청 없는 push 금지.
- 변경 없으면 빈 커밋을 만들지 않는다.
